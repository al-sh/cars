-- V1__init.sql — начальная миграция Flyway.
--
-- Flyway выполняет миграции по порядку: V1, V2, V3, ...
-- Каждая миграция выполняется РОВНО ОДИН РАЗ.
-- Flyway записывает выполненные миграции в таблицу flyway_schema_history.
--
-- Именование: V{номер}__{описание}.sql (два подчёркивания!)
-- V1__init.sql — первая миграция, инициализация.
--
-- Пока пустая — реальные таблицы создадим на следующих этапах.
-- Flyway требует хотя бы одну миграцию при старте.
