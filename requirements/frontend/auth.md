# UI Аутентификации

> **Зависимости:** CONTEXT.md, contracts/api.md, contracts/types.md

Страницы регистрации, входа и управления сессией.

---

## Страницы

### Login (`/login`)

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                      🚗 CarsAI                           │
│                                                          │
│              ┌────────────────────────┐                  │
│              │                        │                  │
│              │  Email                 │                  │
│              │  ┌──────────────────┐  │                  │
│              │  │ user@example.com │  │                  │
│              │  └──────────────────┘  │                  │
│              │                        │                  │
│              │  Пароль                │                  │
│              │  ┌──────────────────┐  │                  │
│              │  │ ••••••••      👁 │  │                  │
│              │  └──────────────────┘  │                  │
│              │                        │                  │
│              │  [      Войти       ]  │                  │
│              │                        │                  │
│              │  Нет аккаунта?         │                  │
│              │  Зарегистрироваться    │                  │
│              │                        │                  │
│              └────────────────────────┘                  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**Поля:**
| Поле | Тип | Валидация |
|------|-----|-----------|
| Email | email | required, email format |
| Пароль | password | required, min 6 символов |

**Поведение:**
- Submit → POST /auth/login
- Success → сохранить токены, редирект по роли
- 401 → "Неверный email или пароль"
- Toggle видимости пароля (иконка глаза)

---

### Register (`/register`)

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                      🚗 CarsAI                           │
│                     Регистрация                          │
│                                                          │
│              ┌────────────────────────┐                  │
│              │                        │                  │
│              │  Имя                   │                  │
│              │  ┌──────────────────┐  │                  │
│              │  │ Иван Петров      │  │                  │
│              │  └──────────────────┘  │                  │
│              │                        │                  │
│              │  Email                 │                  │
│              │  ┌──────────────────┐  │                  │
│              │  │ ivan@example.com │  │                  │
│              │  └──────────────────┘  │                  │
│              │                        │                  │
│              │  Пароль                │                  │
│              │  ┌──────────────────┐  │                  │
│              │  │ ••••••••      👁 │  │                  │
│              │  └──────────────────┘  │                  │
│              │  Минимум 6 символов    │                  │
│              │                        │                  │
│              │  Повторите пароль      │                  │
│              │  ┌──────────────────┐  │                  │
│              │  │ ••••••••      👁 │  │                  │
│              │  └──────────────────┘  │                  │
│              │                        │                  │
│              │  [ Зарегистрироваться ]│                  │
│              │                        │                  │
│              │  Уже есть аккаунт?     │                  │
│              │  Войти                 │                  │
│              │                        │                  │
│              └────────────────────────┘                  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**Поля:**
| Поле | Тип | Валидация |
|------|-----|-----------|
| Имя | text | required, 2-100 символов |
| Email | email | required, email format |
| Пароль | password | required, min 6 символов |
| Повторите пароль | password | required, должен совпадать |

**Поведение:**
- Submit → POST /auth/register
- Success → сохранить токены, редирект на /chat
- 409 → "Email уже зарегистрирован"
- Валидация в реальном времени

---

## Управление сессией

### Хранение токенов

```typescript
// localStorage для refresh_token (persistent)
// memory/sessionStorage для access_token (безопаснее)

interface AuthState {
  user: User | null;
  accessToken: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}
```

### Refresh token flow

```
1. access_token истёк (401 от API)
2. Перехватчик → POST /auth/refresh
3. Success → обновить токены, повторить запрос
4. Fail → logout, редирект на /login
```

### Logout

- Клик на имя пользователя → dropdown menu
- "Выйти" → POST /auth/logout → очистить токены → /login

---

## User dropdown (Header)

```
┌─────────────────────┐
│  Иван Петров     ▼  │
├─────────────────────┤
│  📧 ivan@mail.ru    │
│  ─────────────────  │
│  🚪 Выйти           │
└─────────────────────┘
```

---

## Роутинг и Guards

### Публичные роуты
- `/login`
- `/register`

### Защищённые роуты
- `/chat/**` — требует auth
- `/manager/**` — требует auth + role=manager

### AuthGuard логика

```typescript
canActivate(route) {
  if (!isAuthenticated) {
    redirect('/login', { returnUrl: route.url });
    return false;
  }
  
  if (route.requiredRole && user.role !== route.requiredRole) {
    redirect(getDefaultRoute(user.role));
    return false;
  }
  
  return true;
}

function getDefaultRoute(role: UserRole): string {
  switch (role) {
    case 'client': return '/chat';
    case 'manager': return '/manager';
    case 'admin': return '/admin';
  }
}
```

### Redirect после login

```typescript
// Если есть returnUrl в query params → туда
// Иначе → по роли (client → /chat, manager → /manager)
```

---

## Валидация форм

### Email
- Обязательное
- Формат email (regex или Angular Validators.email)
- Trim пробелов

### Пароль
- Обязательное
- Минимум 6 символов
- Показывать требования под полем

### Подтверждение пароля
- Должен совпадать с паролем
- Валидировать при изменении любого из полей

---

## Состояния форм

| Состояние | UI |
|-----------|-----|
| Pristine | Без ошибок валидации |
| Touched + Invalid | Красная рамка, текст ошибки |
| Submitting | Кнопка disabled, спиннер |
| Success | Редирект |
| Error | Toast или inline error |

---

## Ошибки

| Код | Сообщение |
|-----|-----------|
| 401 | "Неверный email или пароль" |
| 409 | "Пользователь с таким email уже существует" |
| 400 | "Проверьте введённые данные" |
| 500 | "Ошибка сервера, попробуйте позже" |
| Network | "Нет соединения с сервером" |

---

## Дизайн

- Центрированная карточка (max-width: 400px)
- Логотип сверху
- Минималистичный стиль
- Адаптив: на мобильных карточка на всю ширину с отступами

---

## Безопасность

- Не логировать пароли
- HTTPS only в production
- HttpOnly cookies для refresh_token (опционально, альтернатива localStorage)
- CSRF protection при использовании cookies
- Rate limiting на backend для /auth/*
